# Patch package

`opencode-write-json-repair.patch` applies the OpenCode write tool-call JSON repair package.

Use the Windows installer from the package root:

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\install-opencode-write-json-repair.ps1 -OpenCodeRoot C:\src\opencode
```
