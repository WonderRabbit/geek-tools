# Third-Party Notices

이 asset pack은 vendored third-party package를 포함하지 않는다.

사용한 런타임 API:

- Node.js built-in modules: `node:child_process`, `node:path`.
- PowerShell/.NET built-ins: `ConvertFrom-Json`, `ConvertTo-Json`, `Get-FileHash`, `Invoke-WebRequest`, `System.IO.Compression.FileSystem`.

Qwen/OpenAI-compatible endpoint와 OpenCode 자체는 이 repository에 포함되지 않으며, 각 사용 환경의 별도 설치물로 취급한다.

