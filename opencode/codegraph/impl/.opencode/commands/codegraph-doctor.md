---
description: Diagnose the local OpenCode x CodeGraph evidence setup.
---

<command-instruction>
Run `wonder_codegraph_doctor` with the default manifest path.

Summarize:

- whether CodeGraph CLI is available
- whether `.opencode/codegraph/repo-link-manifest.json` exists
- which frontend/backend repo paths are missing
- whether the setup is ready for read-only packet generation

Do not modify files.
</command-instruction>
